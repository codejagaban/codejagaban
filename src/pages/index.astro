---
import Layout from "../layouts/Layout.astro";
import SocialLinks from "../components/SocialLinks.astro";
---

<Layout
  title="Trust Jamin Okpukoro – Senior Technical Writer & Developer Advocate"
  description="Trust Jamin Okpukoro is a senior software engineer, technical writer, and developer advocate focused on building developer-first content, tools, and communities."
  keywords="developer advocate, technical writer, DevRel, Trust Jamin, Trust Okpukoro, open source, API documentation, developer communities"
  image="/image.png"
>
  <section class="hero">
    <div class="container">
      <div class="hero-content animate-fade-in">
        <p class="greeting">Hey 👋🏼, I'm</p>
        <h1 class="name">Trust Jamin</h1>
        <p class="role">Senior Technical Writer & Developer Advocate</p>

        <div class="intro">
          <p>
            I bridge connections between developers and state-of-the-art
            technology. I build <a href="/projects">products</a>, write
            technical documentation and <a href="/blog">tutorials</a>, and
            contribute to
            <a
              href="http://github.com/codejagaban"
              target="_blank"
              rel="noopener noreferrer">open-source</a
            > projects.
          </p>
          <p>
            Currently building <a
              href="https://github.com/codejagaban/techconfafrica"
              target="_blank"
              rel="noopener noreferrer">Tech Conf Africa</a
            >.
          </p>
        </div>

        <div class="cta-section">
          <a href="/about" class="cta-link">
            Learn more about me
            <svg
              width="16"
              height="16"
              viewBox="0 0 16 16"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                d="M6 12L10 8L6 4"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"></path>
            </svg>
          </a>
          <SocialLinks size="md" />
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    padding: var(--space-3xl) 0;
    min-height: 70vh;
    display: flex;
    align-items: center;
  }

  .hero-content {
    max-width: 600px;
  }

  .greeting {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    margin-bottom: var(--space-xs);
  }

  .name {
    font-size: var(--text-4xl);
    font-weight: 600;
    margin: 0;
    background: linear-gradient(
      135deg,
      var(--color-text) 0%,
      var(--color-text-muted) 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .role {
    font-size: var(--text-xl);
    color: var(--color-accent);
    font-weight: 500;
    margin: var(--space-sm) 0 var(--space-xl);
  }

  .intro {
    margin-bottom: var(--space-xl);
  }

  .intro p {
    font-size: var(--text-base);
    line-height: 1.8;
    color: var(--color-text-muted);
  }

  .cta-section {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    align-items: flex-start;
  }

  .cta-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    border: none;
    transition: all var(--transition-base);
  }

  .cta-link:hover {
    color: var(--color-accent);
    border: none;
  }

  .cta-link svg {
    transition: transform var(--transition-fast);
  }

  .cta-link:hover svg {
    transform: translateX(4px);
  }

  /* Highlights Section */
  .highlights {
    display: none;
  }

  .section-title {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-xl);
  }

  /* Feature List - Interactive rows */
  .feature-list {
    display: flex;
    flex-direction: column;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    padding: var(--space-lg) var(--space-md);
    border: none;
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
    text-decoration: none;
  }

  .feature-item:hover {
    background: var(--color-bg);
    border: none;
  }

  .feature-item:hover .feature-icon {
    background: var(--color-accent);
    color: white;
    transform: scale(1.05);
  }

  .feature-item:hover .feature-arrow {
    opacity: 1;
    transform: translateX(0);
    color: var(--color-accent);
  }

  .feature-item:hover h3 {
    color: var(--color-accent);
  }

  .feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: var(--color-bg-hover);
    border-radius: var(--radius-md);
    color: var(--color-text-muted);
    flex-shrink: 0;
    transition: all var(--transition-base);
  }

  .feature-content {
    flex: 1;
    min-width: 0;
  }

  .feature-content h3 {
    font-size: var(--text-lg);
    font-weight: 500;
    margin: 0;
    color: var(--color-text);
    transition: color var(--transition-fast);
  }

  .feature-content p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin: var(--space-xs) 0 0;
  }

  .feature-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-text-subtle);
    opacity: 0;
    transform: translateX(-8px);
    transition: all var(--transition-base);
  }

  @media (max-width: 640px) {
    .feature-item {
      padding: var(--space-md);
    }

    .feature-icon {
      width: 40px;
      height: 40px;
    }

    .feature-arrow {
      display: none;
    }
  }
</style>

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Person",
    "name": "Trust Jamin",
    "url": "https://www.jamin.sh",
    "jobTitle": "Technical Writer and Developer Advocate",
    "sameAs": [
      "https://www.linkedin.com/in/codejagaban",
      "https://github.com/codejagaban",
      "https://dev.to/codejagaban",
      "https://twitter.com/codejagaban",
      "https://blog.jamin.sh"
    ],
    "description": "Trust Jamin is a senior software engineer, technical writer, and developer advocate focused on building developer-first content, tools, and communities."
  }
</script>
